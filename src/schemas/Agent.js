class Agent {
    constructor (accountId, symbol, headquarters, credits, startingFaction) {
        this.accountId = accountId;
        this.symbol = symbol;
        this.headquaters = headquarters;
        this.credits = credits;
        this.startingFaction = startingFaction;
    }
}